# Python
__pycache__/
*.pyc
.pytest_cache/

# Secrets (never commit live keys)
.env

# Runtime data: trade logs, risk ledger, position book, manual halt flag
logs/*.jsonl
bot.log
dash.log
logs/risk_state.json
logs/risk_state.json.tmp
logs/positions.json
logs/positions.json.tmp
logs/KILL

# Backtest cache + reports (regenerate with python -m backtest.harness)
backtest/data/

# Built frontend bundle (shipped to dashboard/dist on the box, never committed)
dashboard/dist/

.vercel
